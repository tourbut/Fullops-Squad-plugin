# Domain docs

기본은 single-context다. 용어집은 `.fullops-squad/docs/design-docs/context.md`, ADR은 `.fullops-squad/docs/design-docs/adr/`에 작성한다.
기존 도메인 문서가 있으면 그 경로를 여기 기록하고 재사용한다. 루트에 중복 CONTEXT.md를 생성하지 않는다.
